#include <stdio.h>

int main()
{
    int num,count,sum;
    printf("\n enter the integer : ");
    for(count>0;count<10;count++)
    {
        scanf("%d",&num);
        sum=num*count;
    }

    {
        printf("\n sum : %d",sum);
    }
    return 0;
}

