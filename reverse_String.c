#include <stdio.h>
#include <string.h>
void main()
{
    int i,j;
    char color[100];
    char shell[100];
    printf("Enter The String: ");
    scanf("%s",&color[i]);
    if(color==32)
    {
      color[i]=color[i]+shell[j];
    }
    strrev(color);
    printf("%s",color);
    int len=strlen(color);
    len=printf("%d",len);
}
