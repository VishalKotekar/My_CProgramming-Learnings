#include <stdio.h>

int main()
{
    int num,count,sum;

    for(count>0;count<10;count++)
    {
        printf("\n enter the integer : ");
        scanf("%d",&num);
        sum=num+sum;
    }

    {
        printf("\n sum : %d",sum);
    }
    return 0;
}
